# fuel-log 開発の入口

作業前に `CLAUDE.md`、`docs/REQUIREMENTS.md`、`docs/ACCEPTANCE.md` を読む。
実装担当はGPT（Codex）。既存のReact・TypeScript・Dexie・PWA構成を維持し、現在の差分から続ける。
本人が明示的に指示するまではcommit・push・pull・rebase・mergeを実行しない。
変更後は単体テスト・本番ビルド・必要な画面確認を行い、実機でしか確認できない項目を区別して記録する。
