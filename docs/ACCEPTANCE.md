# 利用開始前の受け入れ確認

更新日: 2026-09-18 / 実装担当: GPT（Codex）

## 現在地

既存の実装を維持して公開候補を作成した。GitHubリポジトリは未作成。commit・push・Pages公開と実機iPhone確認は未実施。

| 項目 | 状態 | 証拠 |
|---|---|---|
| 自動テスト | 完了 | Node 22.23.2、`npm ci` 後に198件成功（13ファイル） |
| 本番ビルド | 完了 | Node 22.23.2、TypeScriptとViteビルド成功。Service Workerは13ファイル、約769 KiBを事前保存 |
| 記録・計算 | 完了 | 375×812のブラウザで、部分給油200 km/12 L＋満タン300 km/25 L＝13.5 km/Lを確認 |
| 再起動後の保持 | 完了 | 記録4件と表示範囲「半年」が再読込後も保持された |
| オフライン記録 | 開発機で完了 | 配信サーバー停止後、キャッシュから再起動し100 km/10 Lを保存。4件の一覧へ反映 |
| JSON書き出し・復元 | 開発機で完了 | 4件を書き出し→全削除→同ファイルで復元。件数と値が一致 |
| 更新時の入力保護 | 自動検証済み | 新版の到着だけでは再読込せず、本人が「新しい版に更新」へ同意した場合だけ更新。5件の単体テスト |
| GitHub Pages | 未実施 | `AkihiroKunii/fuel-log` はまだ作成されていない |
| 実機iPhone | 未実施 | ホーム画面追加、機内モード、共有シート、更新通知は公開後に確認する |

## 公開後にiPhoneで行う確認

1. Chromeで `https://akihirokunii.github.io/fuel-log/` を開き、ホーム画面へ追加する。
2. ホーム画面の「燃費」から開き、下部が「オフラインで使えます」になるまで待つ。
3. テスト用の通常給油を1件保存し、アプリを閉じて開き直して残っていることを確認する。
4. 機内モードにしてアプリを開き、記録・グラフ・一覧が表示され、もう1件保存できることを確認する。
5. バックアップを書き出し、共有シートから「ファイルに保存」を選ぶ。ファイルアプリにJSONがあることを確認する。
6. テストデータだけの状態で全削除し、そのJSONを「バックアップから復元（置換）」へ入れて、件数と値が戻ることを確認する。
7. 次回の公開時、「新しい版に更新」を入力途中で押さず、保存後に押して更新されることを確認する。

テスト用データは実データと混ぜない。実データが入った後に全削除試験を繰り返さない。

## 公開作業

VaultのGit運用ルールに従い、本人の明示指示後に行う。

1. 変更をローカルcommitする。
2. GitHubに公開リポジトリ `AkihiroKunii/fuel-log` を作成する。
3. `origin`を追加して`main`をpushする。
4. PagesをGitHub Actions方式に設定し、workflowのテスト・ビルド・deploy成功を確認する。
5. 公開URLでアプリ・manifest・Service Workerを確認してから、上の実機確認へ進む。
